<h1>Edit Post</h1>

<%= form_with model: @post, local: true do |form| %>
  <p>Title: <%= form.text_field :title %></p>
  <p>Content: <%= form.text_area :content %></p>
  <%= form.submit 'Update Post' %>
<% end %>

<%= link_to 'Back', posts_path %>

