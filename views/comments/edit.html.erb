<h1>Edit Comment</h1>

<%= form_with(model: [@post, @comment], local: true) do |form| %>
  <p>
    <%= form.label :content %><br>
    <%= form.text_area :content, rows: 5, cols: 40 %>
  </p>

  <%= form.submit 'Update Comment' %>
<% end %>

<%= link_to 'Back to Post', @post %>
